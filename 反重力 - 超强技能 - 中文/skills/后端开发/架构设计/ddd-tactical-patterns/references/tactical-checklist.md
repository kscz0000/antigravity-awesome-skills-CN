# 战术模式检查清单

## 聚合设计

- 每个事务边界对应一个 aggregate root
- 不变量在聚合方法内部强制执行
- 避免跨聚合的同步一致性规则

## 值对象

- 默认不可变
- 构造时进行校验
- 按值相等，而非按标识相等

## 仓储

- 仅持久化和加载 aggregate root
- 暴露面向领域的查询方法
- 避免将 ORM 实体泄漏到领域层

## 领域事件

- 使用过去式的事件命名（例如 `OrderSubmitted`）
- 事件载荷保持最小且稳定
- 破坏性变更前对事件 schema 进行版本管理
