# API 端点构建器

创建完整的、生产级 REST API 端点，包含验证、错误处理、认证和文档。

## 功能说明

此技能创建完整的、生产级 REST API 端点，包含所有必要组件，如具有正确 HTTP 方法的路由处理器、验证、认证、错误处理、响应格式化和文档。

## 使用方法

```
Use @api-endpoint-builder to create a user registration endpoint
```

此技能创建完整的、生产级 REST API 端点，包含所有必要组件，如具有正确 HTTP 方法的路由处理器、验证、认证、错误处理、响应格式化和文档。
