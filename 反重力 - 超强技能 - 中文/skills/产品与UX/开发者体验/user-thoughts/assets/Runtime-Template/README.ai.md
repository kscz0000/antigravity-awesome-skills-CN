# user-thoughts 运行目录

此目录存储 `user-thoughts` 捕获的项目本地用户意图。

## 目录内容

- `define.ini`：技能的运行状态。
- `raw/`：按日期存放的未处理用户想法。
- `ignored/`：用户明确选择不记录的条目。
- `mdbase/`：按维度组织的项目记忆。
- `export/`：导出的 mdbase 内容。

除非用户明确希望丢弃项目记忆，否则不要删除该目录。