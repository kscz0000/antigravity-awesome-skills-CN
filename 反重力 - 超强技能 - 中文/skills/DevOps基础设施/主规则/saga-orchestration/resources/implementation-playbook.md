# Saga 编排实战手册

## 何时选择编排 vs 协同

- 当需要业务流程可见性和集中控制时选择编排。
- 当自治性高且耦合度低时选择协同。

## Saga 设计检查清单

- 定义明确的 Saga 状态机。
- 为每个步骤定义超时策略。
- 为每个不可逆步骤定义补偿动作。
- 为命令处理使用幂等键。
- 在所有事件和日志中存储关联 ID。

## 失败处理

- 使用有界指数退避重试瞬态失败。
- 将不可恢复的失败升级到补偿状态。
- 捕获运维可见的失败原因和当前步骤。

## 验证

- 在每个步骤模拟失败并确认补偿路径。
- 验证重复消息处理。
- 验证编排器重启后的恢复。
