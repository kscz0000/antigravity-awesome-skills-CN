---
name: tdd-workflows
description: "用于处理 TDD 工作流中的 TDD 循环（tdd-workflows-tdd-cycle 的别名）。触发词：TDD工作流、TDD循环、测试驱动开发、tdd cycle"
risk: unknown
source: "alias"
date_added: "2026-06-02"
---

# TDD 工作流（TDD Workflows）

> **本技能是一个别名。** 规范技能是 **`tdd-workflows-tdd-cycle`**。

本技能重定向到 `tdd-workflows-tdd-cycle`，可从资料库加载：

`skill-libraries/testing/tdd-workflows-tdd-cycle/SKILL.md`

## 何时使用

- 当处理 TDD 工作流的 TDD 循环（tdd-workflows-tdd-cycle 的别名）时使用本技能。

## 为什么需要这个别名

用户经常会搜索 `tdd-workflows`，但在本技能集合中完整名称是 `tdd-workflows-tdd-cycle`。这个别名保证了可发现性。

## 局限性

- 仅当任务明确匹配上述范围时使用本技能。
- 不要把输出当作环境特定验证、测试或专家审查的替代品。
- 若缺少必需的输入、权限、安全边界或成功标准，请停下并请求澄清。

## 示例

```text
对当前任务使用 @tdd-workflows：用于处理 TDD 工作流中的 TDD 循环（tdd-workflows-tdd-cycle 的别名）。

把该技能应用到我的当前工作中，并引导我完成最稳妥的下一步、关键检查点，以及我应该产出的具体输出。
```